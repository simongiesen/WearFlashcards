<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_padding"
    android:paddingLeft="@dimen/activity_horizontal_padding"
    android:paddingRight="@dimen/activity_horizontal_padding"
    android:paddingTop="@dimen/activity_vertical_padding"
    tools:context="com.efa.wearflashcards.NewSet">

    <android.support.design.widget.TextInputLayout
        android:id="@+id/new_term"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.Base">

        <EditText
            android:id="@+id/new_term_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/new_term_hint"
            android:inputType="textCapSentences"
            android:theme="@style/AppTheme.Base" />
    </android.support.design.widget.TextInputLayout>

    <android.support.design.widget.TextInputLayout
        android:id="@+id/new_definition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/new_term"
        android:theme="@style/AppTheme.Base">

        <EditText
            android:id="@+id/new_definition_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/new_definition_hint"
            android:inputType="textCapSentences"
            android:theme="@style/AppTheme.Base" />
    </android.support.design.widget.TextInputLayout>

    <Button
        android:id="@+id/done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/new_definition"
        android:layout_below="@+id/new_definition"
        android:onClick="newCard"
        android:text="@string/done" />

    <Button
        android:id="@+id/next"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/new_definition"
        android:layout_toStartOf="@+id/done"
        android:onClick="newCard"
        android:text="@string/next" />

</RelativeLayout>
