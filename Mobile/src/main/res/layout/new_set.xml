<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/list_vertical_padding"
    android:paddingTop="@dimen/list_vertical_padding"
    tools:context="com.efa.wearflashcards.NewSet">

    <android.support.design.widget.TextInputLayout
        android:id="@+id/new_set_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.Base">

        <EditText
            android:id="@+id/new_set_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/set_title_hint"
            android:inputType="textCapWords"
            android:theme="@style/AppTheme.Base" />

    </android.support.design.widget.TextInputLayout>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/new_set_layout"
        android:layout_below="@+id/new_set_layout"
        android:onClick="newSet"
        android:text="@string/create" />

</RelativeLayout>
